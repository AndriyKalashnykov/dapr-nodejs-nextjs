# pnpm config — inherits from root .npmrc but kept here so workspace-local
# pnpm invocations behave identically when run from this directory.
shamefully-hoist=true
auto-install-peers=true
strict-peer-dependencies=false
